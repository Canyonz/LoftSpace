import { SearchInput } from "./SearchInput";

export default {
	title: "shared/SearchInput",
	component: SearchInput,
	tags: ["autodoc"],
};

export const Search = {
	args: {
		type: "search",
	},
};
